[build]
apt = []
pythonVersion = "3.11"

[install]
modules = ["python311", "python311Packages.pip"]

[install.poetry]
enable = true

[start]
cmd = "cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT"